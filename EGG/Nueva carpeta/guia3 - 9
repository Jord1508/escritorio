Algoritmo sin_titulo
//	Escribir un programa que procese una secuencia de caracteres ingresada por teclado y
//terminada en punto, y luego codifique la palabra o frase ingresada de la siguiente manera:
//	cada vocal se reemplaza por el carácter que se indica en la tabla y el resto de los caracteres
	//	(incluyendo a las vocales acentuadas) se mantienen sin cambios.
	Definir palabra, z, convertir Como Caracter
	Definir x, i Como Real
		Escribir "Ingrese una frase/palabra que termine con un punto: "
		Leer palabra
		x=Longitud(palabra)
		z = SubCadena(palabra,x-1,x-1)
		Mientras z <> "." Hacer
			Escribir "Ingrese un frase/palabra nuevamente: "
			Leer palabra
			x=Longitud(palabra)
			z = SubCadena(palabra,x-1,x-1)
		FinMientras
		carac(palabra, x, z, convertir)
		Mostrar convertir
FinAlgoritmo

SubProceso carac(palabra, x, z Por Referencia, convertir Por Referencia)
	Definir i Como Real
	convertir=""
	Para i<-0 Hasta x Con Paso 1 Hacer
		z = SubCadena(palabra,i,i)
			Segun z Hacer
				"a":
					convertir=Concatenar(convertir,"@")
				"e":
					convertir=Concatenar(convertir,"#")
				"i":
					convertir=Concatenar(convertir,"$")
				"o":
					convertir=Concatenar(convertir,"%")
				"u":
					convertir=Concatenar(convertir,"*")
				De Otro Modo:
					convertir=Concatenar(convertir,z)
			Fin Segun
		Fin Para
	FinSubProceso